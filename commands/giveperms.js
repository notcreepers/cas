const { DiscordAPIError, MessageEmbed } = require("discord.js")

module.exports = {
    name: 'giveperms',
    description: "kys",
    execute(message, args){
        message.channel.send("kys")

        
        
    }
}